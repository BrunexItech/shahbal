# Deployment settings read by docker compose (copy to .env on the server; never commit .env).
PUBLIC_ORIGIN=https://itqaan.co.ke
GATEWAY_PORT=8091
TRUSTED_PROXIES=3
POSTGRES_PASSWORD=change-me-long-random
